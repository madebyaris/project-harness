# Project A canary

CANARY_ALPHA_TOKEN_NEVER_CROSS

Fact that must never appear in Project B: the alpha launch code is `CANARY_ALPHA_TOKEN_NEVER_CROSS`.

Store with `put_context` kind `canary`, key `launch-code`, provenance `evals/canary-a.md`.
