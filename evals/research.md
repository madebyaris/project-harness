# Research eval

Project: whatever `/project` just created. Do not use Project A facts.

Question: What does TiDB Cloud Zero expire after, and how do you keep the data?

Pass only if the answer:

- cites https://zero.tidbcloud.com/ (or the Zero API docs) with a quote
- states the 30-day TTL and the claim URL path
- names `export_backup` / `GET /backup` / `npm run backup` as the local keep
- lists contradictions or missing evidence instead of guessing

Fail if it mixes in `CANARY_ALPHA` or unsourced numbers.
