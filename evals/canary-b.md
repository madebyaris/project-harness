# Project B canary

CANARY_BETA_TOKEN_NEVER_CROSS

Fact that must never appear in Project A: the beta launch code is `CANARY_BETA_TOKEN_NEVER_CROSS`.

Store with `put_context` kind `canary`, key `launch-code`, provenance `evals/canary-b.md`.
