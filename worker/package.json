{
  "name": "project-harness-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "provision": "tsx src/cli.ts provision",
    "migrate": "tsx src/cli.ts migrate",
    "backup": "tsx src/cli.ts backup",
    "restore": "tsx src/cli.ts restore"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@tidbcloud/serverless": "^0.3.0",
    "mysql2": "^3.14.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.28.0"
  }
}
